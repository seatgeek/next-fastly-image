# Description

<!-- What does this PR change, and why? -->

## Checklist

- [ ] `make check` passes locally (typecheck, lint, tests, build, export checks)
- [ ] New behavior is covered by tests
- [ ] Option keys still map 1:1 to Fastly IO query parameter names
- [ ] No `next` import in `src/index.ts` / `src/next.ts` (zero-dependency invariant)
- [ ] Added a changeset (`pnpm changeset`) if this should trigger a release
- [ ] README/docs updated if user-facing behavior changed
