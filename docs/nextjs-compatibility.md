# Next.js compatibility

- **Primary target: Next.js 16.x; Next.js 13-15 verified in CI.** Every PR type-checks the loader contract against `next/image`'s own `ImageLoader`/`ImageProps` types and runs the full test suite - including the `FastlyImage` render tests - against Next 13, 14 (with React 18), and 15 (React 19) in a dedicated `compat` matrix; the primary job covers Next 16. The custom-loader contract, `({ src, width, quality }) => string`, is unchanged across all supported versions, and there is zero runtime coupling to `next` outside the `/component` entry.
- **App Router:** pass `loader` props only from Client Components; the global `loaderFile` has no client/server constraint ([vercel/next.js#41924](https://github.com/vercel/next.js/issues/41924)).
- **Next 16 `images.qualities`:** defaults to `[75]` and acts as an allowlist. A `quality` prop not in the list is **silently coerced to the closest allowed value** *before* your loader sees it (`quality={60}` arrives as `75` unless you set `images: { qualities: [60, 75] }`). Preset qualities are unaffected - they're applied inside the loader, after Next.js.
- **`/_next/image` does not exist under `loader: "custom"`** - Next.js skips bootstrapping the built-in optimizer endpoint entirely (requests 404, verified on 16.3.1), so a global loader cannot hand traffic back to it. Mixed setups belong at the [component level](./recipes.md#using-multiple-loaders).
- **`remotePatterns` is not required** for remote srcs when `loader: "custom"` is set - that allowlist guards the built-in `/_next/image` endpoint, which your Fastly-routed images never touch. (You still need it for any images left on the built-in optimizer in mixed setups.)
- **Unchanged-src inputs trigger a dev warning:** srcs this package returns unchanged (no leading slash, non-http(s) schemes, unparseable strings) surface in Next dev mode as `loader ... does not implement width` with a degenerate srcset - the image loads, untransformed.
